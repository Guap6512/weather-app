export interface UnitsState {
    selectedUnit: Units;
}

export enum Units {
    CELSIUS = 'CELSIUS',
    FAHRENHEIT = 'FAHRENHEIT',
}

export enum MetricalSystems {
    METRIC = 'metric',
    IMPERIAL = 'imperial',
}
